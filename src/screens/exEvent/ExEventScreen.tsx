import { View, Text, Image,Pressable } from 'react-native'
import React from 'react'
import { SafeAreaView } from 'react-native-safe-area-context'
import Slider from '../../component/Slider'

const ExEventScreen = () => {
  return (
    <SafeAreaView>
      <Slider/>
    </SafeAreaView>
  )
}

export default ExEventScreen 